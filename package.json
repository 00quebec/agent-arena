{
  "name": "agent-arena",
  "version": "0.1.0",
  "description": "Run competitive coding-agent matches in isolated worktrees.",
  "type": "module",
  "bin": {
    "agent-arena": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
